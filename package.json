{
  "name": "energy-tracker",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@salutejs/client": "^1.37.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "recharts": "^2.8.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "preview": "npx --yes serve -s build",
    "pack:canvas": "node scripts/pack-canvas.js",
    "pack:scenario": "node scripts/pack-scenario.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}